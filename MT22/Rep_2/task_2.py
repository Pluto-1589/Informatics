def length(iterable):
    # The function should compute the length of iterable recursively. For example, [1, 2, [3, 4], True], ("a", 1, 2, None) and "good" all have a length of 4.
    pass


print(length([1, 2, [3, 4]]))
print(length(("a", 1, 2, None)))
print(length("oh dear"))
